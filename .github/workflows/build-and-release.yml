name: Build and release
run-name: ${{ github.actor }} is releasing the newest version.
on:
  push:
    branches: [main]
jobs:
  build-and-release:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - run: java --version

      - name: Build with Maven
        run: |
          chmod +x ./cicd/build.sh
          ./cicd/build.sh

      - run: echo "$(git log | git shortlog)"

      - name: Release the build
        run: |
          tag="$(date +"%Y.%m.%d")-${{ github.sha }}"
          git shortlog --since=2024-03-10T00:00 > release-notes.txt
          gh release create $tag ./dist/*-runner --title "franklyn $tag" --notes-file release-notes.txt --prerelease
          git tag --sort=refname
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
